# / --------------------------------------------------------------------- \
#                           i3 config file 
# \ --------------------------------------------------------------------- /

# modifier key
set $mod Mod4

# default programs
set $web_browser firefox
set $file_manager pcmanfm
set $text_editor "st -e 'nvim'"
set $system_monitor "st -e 'htop'"

# font
font pango:DejaVu Sans Mono 12

# autostart
exec --no-startup-id "xrdb -merge '~/.Xresources'" # x settings
exec --no-startup-id nm-applet # network connections manager
exec --no-startup-id "picom -f" # compositor
exec --no-startup-id blueman-applet # bluetooth manager
exec --no-startup-id "nitrogen --restore" # wallpapers
exec --no-startup-id pa-applet # volume
exec --no-startup-id xfce4-power-manager # power settings and events
exec --no-startup-id "udiskie -t" # device auto mounter
exec --no-startup-id caffeine # disable power saving temporarely
exec --no-startup-id redshift-gtk # color temperature adjuster
exec --no-startup-id "xss-lock i3lock-fancy-multimonitor" # screen lock
exec --no-startup-id /usr/lib/xfce4/notifyd/xfce4-notifyd # notifications
exec --no-startup-id gxkb # kbd layout switcher

# Use pactl to adjust volume in PulseAudio.
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Brightness function keys
bindsym XF86MonBrightnessUp exec --no-startup-id "xbacklight -inc 10"
bindsym XF86MonBrightnessDown exec --no-startup-id "xbacklight -dec 10"

# Display function key
bindsym XF86Display exec --no-startup-id arandr

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id st

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run -fn "DejaVu Sans Mono:pixelsize=18"

# quick launch mode
set $quick_launch Quick launch: web [b]rowser, [f]ile manager, text [e]ditor, [s]ystem monitor
mode "$quick_launch" {
    bindsym b exec $web_browser
    bindsym f exec $file_manager
    bindsym e exec $text_editor
    bindsym s exec $system_monitor

    bindsym Escape mode "default"
    bindsym Return mode "default"
}
bindsym $mod+a mode "$quick_launch"

# screenshot
bindsym Print exec --no-startup-id "scrot '%Y-%m-%d_$wx$h_scrot.png' -e 'mv $f ~/Pictures/screenshots/'"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+x split h

# split in vertical orientation
bindsym $mod+z split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# set workspace numbers
set $ws1 1
set $ws2 2
set $ws3 3
set $ws4 4
set $ws5 5
set $ws6 6
set $ws7 7

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Escape exec "i3-msg exit"

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# borders
hide_edge_borders smart
new_window 1pixel

# gaps
gaps inner 2
gaps inner 5

# status bar
bar {
        position top
        status_command i3blocks
}
